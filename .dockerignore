# .dockerignore — Phoenix-Evasion-Research (QurolVoV)
# Optimized for multi-stage Docker build

# Git
.git/
.gitignore
.gitattributes

# Python cache files
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/

# Build and distribution (keep source files)
develop-eggs/
downloads/
eggs/
.eggs/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Testing and coverage (KEEP tests/ directory for builder stage)
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.mypy_cache/
.ruff_cache/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
*.sublime-project
*.sublime-workspace

# CI/CD (exclude workflows but keep config files)
.github/workflows/

# Documentation (KEEP these files for build)
# docs/  # Keep if needed for build

# Logs and temporary files
*.log
*.bak
*.tmp

# Environment files (sensitive data)
.env
.env.local
.env.*.local

# OS
Thumbs.db
.AppleDouble
.LSOverride

# Docker (keep these for build context)
# Dockerfile - KEEP for build
# .dockerignore - KEEP for build
# docker-compose.yml - KEEP if needed

# Development configs (KEEP these for build)
# .pre-commit-config.yaml
# pyproject.toml
# setup.cfg
