# requirements-dev.txt
# Development & CI/CD dependencies untuk Phoenix-Evasion-Research Framework
# Instal dengan: pip install -r requirements-dev.txt

# === Testing & Coverage ===
pytest>=8.3.0
pytest-cov>=5.0.0
pytest-xdist>=3.6.0          # Parallel testing
pytest-mock>=3.14.0

# === Code Quality & Formatting ===
black>=24.8.0                # Code formatter (wajib!)
ruff>=0.9.0                  # Super-fast linter pengganti flake8 + isort + pyupgrade
isort>=5.13.0                # Sort imports (jika tidak pakai ruff)

# === Static Type Checking ===
mypy>=1.11.0
types-psutil>=5.9.5
types-requests>=2.32.0

# === Security Scanning ===
bandit>=1.7.9                # SAST untuk Python
safety>=3.2.0                # Cek vulnerability di dependencies
semgrep>=1.88.0              # Advanced SAST (rules keamanan siber)

# === Pre-commit Hooks ===
pre-commit>=3.8.0

# === Documentation ===
mkdocs>=1.6.0
mkdocs-material>=9.5.0
mkdocstrings[python]>=0.26.0

# === Utilities ===
rich>=13.8.0                 # Beautiful terminal output
tqdm>=4.66.0                 # Progress bar

# === Optional: Untuk bounty hunter & kontributor ===
hypothesis>=6.108.0          # Property-based testing (advanced)
faker>=28.0.0                # Generate fake data untuk testing

# === CI/CD Extra ===
twine>=5.1.0                 # Untuk publish ke PyPI nanti (opsional)
build>=1.2.0
